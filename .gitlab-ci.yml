image: maven:3-jdk-8

before_script:
  - apt-get --quiet update --yes
  - apt-get --quiet install openssh-client --yes

build:
  script: "mvn package -B"

deploy:
  script: "mvn clean package -B && scp target/telegram-plan.1.0-SNAPSHOT.jar $sshUser@$sshPassword:/home/$sshUser/."
